{{flutter_js}}
{{flutter_build_config}}

// Pocket Party uses its own service_worker.js. Flutter's generated worker is
// intentionally not registered here because its generation is deprecated.
_flutter.loader.load();
